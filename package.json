{
  "name": "pushfire-firebase-extension",
  "description": "PushFire Firebase Extension",
  "version": "0.2.0",
  "author": "PushFire, a product of Flywheel Studio. Ender Puentes <ender@flywheel.so>",
  "private": false,
  "keywords": [
    "pushfire",
    "firebase",
    "extension",
    "sync",
    "subscribers",
    "firestore"
  ],
  "license": "Apache-2.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "commitlint": "commitlint --edit ${1} --config ./commitlint.config.json",
    "release": "release-it --ci",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=18"
  },
  "main": "index.js",
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@release-it/bumper": "^7.0.5",
    "@release-it/conventional-changelog": "^10.0.2",
    "husky": "^9.1.7",
    "release-it": "^19.0.6"
  }
}
